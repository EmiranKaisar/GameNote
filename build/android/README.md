# Android packages

After the one-time `npm run mobile:init:android`, run `npm run build:android` from `app/`. Ignored local `.apk` and/or `.aab` packages are collected here. Use an APK for direct testing and an appropriately signed AAB for Google Play publishing.
