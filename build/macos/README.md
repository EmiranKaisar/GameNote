# macOS packages

Run `npm run build:mac` from `app/` on macOS. The ignored local `Game Note.app` package is collected here. For a distributable GitHub Release asset, use `npm run build:mac:dmg` and upload the resulting DMG.
