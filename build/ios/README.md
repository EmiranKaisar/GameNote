# iOS packages

After the one-time `npm run mobile:init:ios`, run `npm run build:ios` from `app/` on macOS. Ignored local iOS packages are collected here; use the configured signing workflow to distribute through TestFlight or App Store Connect.
