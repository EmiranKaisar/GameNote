# Windows packages

Run `npm run build:windows` from `app/` on Windows. The ignored local `.msi` and/or `.exe` installers are collected here; upload finished installers to the matching GitHub Release.
