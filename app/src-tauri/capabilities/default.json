{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the Game Note main window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
